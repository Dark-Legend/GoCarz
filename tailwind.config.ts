import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        btnColor: "#007AFF",
      },
    },
  },
  plugins: [],
};

export default config;
